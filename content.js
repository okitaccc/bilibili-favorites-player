let video;
function report(extra={}){if(video)chrome.runtime.sendMessage({type:'mediaState',bvid:location.pathname.match(/\/video\/(BV[\w]+)/i)?.[1],videoTitle:document.querySelector('h1.video-title,.video-title')?.textContent?.trim()||document.title.replace(/_哔哩哔哩_bilibili$/,''),cover:document.querySelector('meta[property="og:image"]')?.content,paused:video.paused,currentTime:video.currentTime,duration:video.duration||0,volume:video.muted?0:video.volume,blocked:false,...extra})}
async function tryPlay(){try{await video.play();report()}catch{report({blocked:true})}}
function attach(){const found=document.querySelector('video');if(!found||found===video)return;video=found;video.addEventListener('ended',()=>chrome.runtime.sendMessage({type:'ended'}));video.addEventListener('play',report);video.addEventListener('pause',report);video.addEventListener('timeupdate',report);video.addEventListener('volumechange',report);tryPlay()}
chrome.runtime.onMessage.addListener(m=>{if(!video)return;if(m.type==='toggle')video.paused?tryPlay():video.pause();if(m.type==='seek')video.currentTime=video.duration*m.ratio;if(m.type==='volume'){video.muted=m.value===0;video.volume=m.value}});attach();new MutationObserver(attach).observe(document.documentElement,{childList:true,subtree:true});
